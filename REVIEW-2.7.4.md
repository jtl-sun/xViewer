# xViewer 2.7.4 검토·수정·검증 결과

검토 기준: 제공된 xViewer 2.7.0 소스 ZIP과 2.7.4 개발 가이드의 목표. 문서의 과거 버전 설명이나 권고는 현재 프로그램의 검증 결과로 취급하지 않았다.

## 확인한 문제와 수정

| 확인한 문제 | 적용한 수정 |
|---|---|
| Excel 시작·열기·페이지 설정·내보내기·닫기가 무제한으로 멈출 수 있음 | 별도 프로세스가 Excel COM을 담당하고 부모가 전체 작업에 15초 제한 적용. 실패/취소 시 해당 작업과 확인된 전용 Excel 핸들만 종료 |
| 이전 파일과 이웃 파일 미리읽기가 최신 선택을 막음 | 자동 prefetch 중단, 실행 중 요청 취소, 대기 요청은 최신 1개만 유지 |
| 변환 오류가 작업 스레드를 종료시키거나 Tk에 전달되지 않음 | 작업 전체 예외 처리, Tk 전용 결과 큐, 예외 객체를 콜백에 안전하게 보관 |
| PDF·이미지 선택 시 초기화하지 않은 delay 사용 | 파일 종류별 지연 초기화, Excel 220ms/캐시 45ms 적용 |
| `.xls` 이름의 실제 OOXML 파일을 xlrd로 읽다가 실패 | 내부 파일 서명으로 형식을 확인하고 잘못된 확장자의 OOXML도 읽기 전용으로 처리 |
| PDF 실패 후 오래된 XLS COM 변환을 반복 | 호환 보기에서 COM 재호출 금지, 호환 보기 읽기 전용 및 로딩 종료 처리 |
| PDFium 동시 호출과 반복 렌더 스레드 증가 | PDFium 호출 잠금, 최신 작업 대기열, 렌더 픽셀 수 제한 |
| 불완전 PDF/변환 도중 원본 변경/손상 캐시 | PDF 앞·끝 확인, 고유 임시 파일, 원본 메타데이터 재검사, 원자적 교체, 손상 캐시 제거 후 호환 보기 |

원본 파일에 저장하지 않는다. 사용자 Excel 전체를 프로세스 이름으로 종료하는 명령은 사용하지 않는다. Excel 프로세스 ID를 받기 전 자동화 시작 단계가 멈추면, 소유권을 확인할 수 없는 Excel을 추측하여 종료하지 않고 작업 프로세스만 종료한다.

## 2.7.4 기능 목표 반영

- `.xls/.xlsx/.xlsm/.xltx/.xltm`의 Excel PDF 우선 미리보기와 외부 Excel 편집.
- PrintArea 초기화, Zoom 해제, 가로·세로 1페이지 맞춤. 방향·용지·여백 유지. Find/Shapes 탐색 제거.
- PageSetup 실패에는 원래 설정으로 한 번 재시도. 다른 Excel 실패에는 설치된 LibreOffice를 사용하며 추가 작업도 최대 15초로 제한.
- 일반 PDF 및 Excel PDF의 기본 Fit과 1:1/Fit 전환. 이미지 Fit 최대 100%.
- Excel PDF만 표시용 흰 여백 축소. 실제 PDF 파일은 그대로 유지.
- Excel/PDF/Images 독립 필터의 합집합. 모두 해제하면 모든 형식 표시.
- TXT/MD/Markdown/CSV/JSON 읽기 전용, 선택/전체 복사, 가로·세로 스크롤, JSON 정렬.
- 캐시 버전 갱신, 원본 경로·크기·수정시간 식별, 약 750개/3GB 상한 정리.
- 단계별 로컬 로그와 `diagnose_excel.bat` 제공.

## 실제 검증 결과

검증 환경: Windows, Python 3.12. 별도 시험 환경에서 수행했으며 사용자 Excel 원본은 쓰지 않았다.

| 검사 | 결과 | 의미/범위 |
|---|---|---|
| 자동 회귀 검사 | 66개 통과, 건너뜀 없음 | 기존 기능, 실제 중단된 자식 프로세스의 제한시간/취소/재시작, 최신 요청 우선, 오류 복구, 캐시, 형식 판별 포함 |
| 숨긴 Tk 창 통합 검사 | 위 검사에 5개 포함 | 실제 이벤트 루프에서 PDF·이미지·텍스트 로드, 실패 전달, Fit 전환, 호환 보기, 오래된 결과 무시, 손상 캐시 복구 |
| 실제 Excel 샘플 | 102/102 읽기 성공 | `.xls` 이름 86개와 `.xlsx` 이름 16개. Python 호환 보기로 시트와 셀을 읽은 결과이며 Excel PDF 시각 재현 검사가 아님 |
| 원본 보존 | 102/102 동일 | 검사 전후 SHA-256 비교 |
| 실제 PDF 샘플 | 10/10 첫 페이지 렌더 성공 | 모든 페이지의 시각 검사는 아님 |
| 모듈 컴파일 및 --check | 통과 | 의존성/모듈 검사. --check는 Office 실변환 성공을 뜻하지 않음 |

샘플 중 확장자 `.xls`와 내부 OOXML 형식이 달라 실패하던 1개를 수정 후 다시 읽는 데 성공했다. 일부 샘플의 WMF/EMF는 Python 호환 보기에서 여전히 생략된다. 모든 XLS 그림을 Python 라이브러리로 복원했다고 주장하지 않는다.

## 아직 필요한 실제 사용자 세션 검증

Excel COM 변환은 XLS/XLSX 각각 한 파일에서 Windows 오류 `0x80070520`(지정한 로그온 세션이 없음)으로 시작 단계에서 실패했다. 이 자동 작업 환경에서는 Excel이 생성한 PDF의 사진·도형·인쇄 배치와 실제 변환 시간을 검증하지 못했다. LibreOffice 실변환도 이번 환경에서 확인하지 못했다.

설치 후 `diagnose_excel.bat`에서 문제가 있던 파일을 선택하면 실제 로그인된 Windows 세션에서 검사할 수 있다. 결과는 `%LOCALAPPDATA%\xViewer\logs`에 남고 성공 시 캐시 PDF 경로도 기록된다. PDF와 Excel 원본을 비교해 사진·방향·페이지 수를 확인해야 한다.

긴 시트를 세로 1페이지로 맞추면 글자가 작아질 수 있다. 호환 보기는 서식·도형·캐시된 수식 결과를 완전히 재현하지 못할 수 있다. 호환 읽기가 15초를 넘으면 화면은 오류로 전환되지만 실행 중인 Python 읽기 자체는 강제 종료하지 않는다(대기열은 최신 1개로 제한). 텍스트는 자동 인코딩 판별의 한계가 있으며 앞부분 16 MiB만 표시한다.

## 적용과 되돌리기

기존 xViewer를 종료하고 이 폴더의 `install_xViewer.bat`를 실행하면 기존 바로가기에서 새 버전이 실행된다. 소스 폴더 변경만으로 이미 설치된 Python 패키지가 자동 갱신되지는 않는다.

원래 2.7.0 ZIP은 보관한다. 이전 버전으로 돌아가려면 그 ZIP을 별도 폴더에 풀어 설치한다. 새 캐시는 버전별 키가 달라 이전 캐시와 혼동하지 않는다.
